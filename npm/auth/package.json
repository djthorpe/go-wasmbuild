{
    "name": "go-wasmbuild-auth",
    "private": true,
    "devDependencies": {
        "esbuild": "^0.25.0"
    },
    "scripts": {
        "build": "esbuild auth.ts token.ts --bundle --format=esm --platform=browser --target=es2020 --outdir=${OUTDIR:-.} --minify"
    }
}